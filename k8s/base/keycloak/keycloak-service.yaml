apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: jbpm
  labels:
    app.kubernetes.io/name: keycloak
    app.kubernetes.io/part-of: jbpm-stack
spec:
  type: ClusterIP
  ports:
    - port: 9080
      targetPort: 9080
      name: http
  selector:
    app.kubernetes.io/name: keycloak
